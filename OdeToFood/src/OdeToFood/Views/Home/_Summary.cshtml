@model Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        Cuisine: @Model.Cuisine
    </div>
    <div>
        <a asp-action="Detail" asp-route-id="@Model.Id">View</a> |
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
    </div>
</section>